% Calculating trigonometric functions
clear all;
clc;            % Clear screen

angle = pi / 2;

sinVal = sin(angle);
cosVal = cos(angle);
tanVal = tan(angle);
ctgVal = cot(angle);

someExp = exp(5);

someLog = log(someExp);

root4 = sqrt(4);

root8 = nthroot(3, 8);

absoluteVal = abs(-5);